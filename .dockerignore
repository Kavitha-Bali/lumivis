.git
.gitignore
venv/
env/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

.env
.env.example
requirement.txt

db.sqlite3
media/
staticfiles/

# Static source assets (logos, product images, video.mp4/video3.mp4, etc.)
# are only needed to run `collectstatic`, which now happens as a separate
# deploy step against Azure Blob Storage — not inside the image. Excluding
# them keeps the build context/image small.
products/static/

.vscode/
.idea/
.DS_Store
